# Core - Stable versions
pyyaml>=6.0
pydantic>=2.0.0
python-dotenv>=1.0.0
loguru>=0.7.0

# Database
sqlalchemy>=2.0.0,<3.0.0

# Security & PII detection
cryptography>=41.0.0
phonenumbers>=8.13.0

# Optional: Presidio (comment out if fails)
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0

# Local LLM - Ollama (pure Python, should work)
ollama>=0.1.0

# HuggingFace fallback - only if you need it
# transformers>=4.35.0
# torch>=2.1.0
# sentencepiece>=0.1.99
# accelerate>=0.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
faker>=20.0.0

# HTTP client
aiohttp>=3.9.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6